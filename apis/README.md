APIs for Stellar derivatives/DEX data service. Endpoints:
- GET /prices/:token
- GET /markets
- GET /markets/:market_id
- GET /positions/:account

Use mock Reader implementations for tests under apis/tests.
